{
  "name": "dog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start":"node",
    "start:docker": "docker-compose -f docker/docker-compose.yml up -d",
    "start:docker:loud": "docker-compose -f docker/docker-compose.yml up",
    "stop:docker" : "docker-compose -f docker/docker-compose.yml stop",
    "clean:docker" : "docker-compose -f docker/docker-compose.yml drop",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fluent-logger": "^2.0.1",
    "lodash": "^4.16.2"
  }
}
